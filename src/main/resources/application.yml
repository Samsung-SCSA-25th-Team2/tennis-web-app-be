spring:
  datasource:
    url: jdbc:mysql://db-tennis-web-app.cnqkisgwa8vz.ap-northeast-2.rds.amazonaws.com:3306/db-tennis-web-app?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: admin
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      default_batch_fetch_size: 100 # 쿼리 한번에 대한, row batch_size
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true  # SQL 스크립트가 Hibernate 이후 실행되도록 설정

  sql:
    init:
      mode: always  # schema.sql 항상 실행
      continue-on-error: true  # 테이블이 이미 존재해도 계속 진행

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: "a5684086cef2a24799efaf9268549f00"
            client-secret: "Gt5Qf0HaRcS5FlIgvUs5niSc4pdEWbBq"
            redirect-uri: "{baseUrl}/login/oauth2/code/kakao"
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            scope:
              - profile_nickname # 프로필 명
              - profile_image # 프로필 사진
            client-name: Kakao
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8080

logging:
  level:
#        org.springframework.boot: ERROR
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.springframework.data.jpa: DEBUG
#    com.zaxxer.hikari: DEBUG
#    org.hibernate: DEBUG

jwt:
  secret: scsa-25th-team2-forever-jwt-secret-key-for-development-only-minimum-256-bits-required
  access-token-expiration: 600000  # 10분 (Access Token)
  refresh-token-expiration: 604800000  # 7일 (Refresh Token)

redis:
  host: redis-14621.c340.ap-northeast-2-1.ec2.cloud.redislabs.com
  port: 14621
  password: "azycRtr20NyWTwdpkjZ0tFRNNbnYeh0c"

# SpringDoc OpenAPI (Swagger) 설정
springdoc:
  api-docs:
    path: /v3/api-docs  # OpenAPI 3.0 문서 경로
  swagger-ui:
    path: /swagger-ui.html  # Swagger UI 접속 경로
    tags-sorter: alpha  # 태그를 알파벳 순으로 정렬
    operations-sorter: alpha  # API를 알파벳 순으로 정렬
    display-request-duration: true  # 요청 처리 시간 표시
    try-it-out-enabled: true  # Try it out 버튼 활성화
  show-actuator: false  # Spring Actuator 엔드포인트 숨김
